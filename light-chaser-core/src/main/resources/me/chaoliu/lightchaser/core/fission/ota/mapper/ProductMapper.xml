<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.chaoliu.lightchaser.core.fission.ota.mapper.ProductMapper">
    <select id="selectProductByID" parameterType="int" resultType="Product">
        select * from product where id = #{id}
    </select>

    <!-- 对应ProductMapper中的insertProduct方法  -->
    <insert id="insertProduct" parameterType="Product">
        insert into product(product_name, product_id, update_time, destination)
        values(#{productName}, #{productId}, #{updateTime}, #{destination})
    </insert>

</mapper>